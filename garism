cli.Activate()

; --- Get last used row ---
getLastRow := cli.Cells(cli.Rows.Count, 1).End(-4162).Row

; --- Loop from row 2 to last row ---
for row in 2..getLastRow {
    ; Column G = column 7
    colGValue := cli.Cells(row, 7).Value

    AddToLog("Row " row " → G: " colGValue)
}
